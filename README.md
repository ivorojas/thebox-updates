# THE BOX — canal de actualizaciones

Parches del juego (pck compilado, ~7MB, SIN modelos de IA ni codigo fuente).
El juego los baja solo desde el menu (BUSCAR ACTUALIZACIONES).

- `manifest.json` — version actual + hash
- `patch.pck` — el parche
